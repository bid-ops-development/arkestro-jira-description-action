pre-commit:
  commands:
    bundle:
      run: yarn build:bundle
      stage_fixed: true
    has_changes:
      run: git diff --exit-code dist/index.js
      fail_text: changes found after running yarn build:bundle make sure to commit these
